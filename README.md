# SkyPro Wallet

Приложение для управления личными расходами с возможностью анализа и визуализации данных.

## Описание проекта

SkyPro Wallet - это веб-приложение на React для отслеживания и анализа личных расходов. Приложение включает аутентификацию пользователей, управление расходами и инструменты для анализа финансовых данных.

## Технологии

- **Frontend**: React 19, Vite
- **Стилизация**: Styled Components
- **Маршрутизация**: React Router DOM v7
- **HTTP-клиент**: Axios
- **Качество кода**: ESLint, Prettier

## Выполненные задачи

### ✅ Настройка проекта

- Инициализация проекта с Vite
- Настройка React 19
- Установка и конфигурация Styled Components
- Настройка React Router для навигации
- Добавление ESLint и Prettier для качества кода

### ✅ Аутентификация пользователей

- Создание контекста аутентификации (AuthContext)
- Реализация хука useAuth для доступа к состоянию аутентификации
- Защита маршрутов с помощью PrivatePage компонента
- Создание страниц входа и регистрации
- Реализация переключения между формами входа/регистрации в одном компоненте

### ✅ Структура приложения

- Настройка маршрутизации с защищенными и публичными маршрутами
- Создание основного layout с Header и Hero компонентами
- Реализация навигации в Header (логотип, ссылки на разделы, выход)

### ✅ Страницы приложения

- **Главная страница** (/): отображает страницу расходов
- **Мои расходы** (/expenses): таблица расходов и форма добавления новых
- **Анализ расходов** (/analysis): календарь и гистограмма для анализа
- **Вход** (/signin): форма аутентификации
- **Регистрация** (/signup): форма регистрации

### ✅ Компоненты интерфейса

- Header: навигационная панель с логотипом и ссылками
- Hero: основной контейнер для контента страниц
- Login: универсальный компонент для входа и регистрации
- ExpensesLayout: layout для страницы расходов
- AnalysisLayout: layout для страницы анализа
- ExpenseTable: компонент для отображения таблицы расходов с фильтрами и данными
- NewExpense: компонент для добавления и редактирования новых расходов
- Filter: компонент для сортировки расходов по дате и сумме
- FilterCategory: компонент для фильтрации расходов по категориям
- Calendar: компонент календаря для анализа (заглушка)
- Histogram: компонент гистограммы для визуализации (заглушка)

### ✅ Стилизация

- Глобальные стили приложения
- Стилизованные компоненты для всех элементов интерфейса
- Адаптивный дизайн с использованием Container и Wrapper

### ✅ API и данные

- Реализация API функций для аутентификации (authApi.js)
- Добавление примерных данных расходов (data.js)

### ✅ Функциональность таблицы расходов

- Реализация полной таблицы расходов с отображением данных
- Добавление фильтров по категориям (FilterCategory)
- Добавление сортировки по дате и сумме (Filter)
- Поддержка редактирования и удаления расходов в таблице

### ✅ Форма расходов

- Реализация формы добавления новых расходов
- Поддержка редактирования существующих расходов в NewExpense

## Запуск проекта

1. Установите зависимости:

   ```bash
   npm install
   ```

2. Запустите приложение в режиме разработки:

   ```bash
   npm run dev
   ```

3. Откройте браузер и перейдите по адресу `http://localhost:5173`

## Скрипты

- `npm run dev` - запуск сервера разработки
- `npm run build` - сборка проекта для продакшена
- `npm run lint` - проверка кода ESLint
- `npm run preview` - предварительный просмотр собранного приложения

## Структура проекта

```
src/
├── api/                 # API функции
│   └── authApi.js       # Функции аутентификации
├── components/          # Переиспользуемые компоненты
│   ├── AnalysisLayout/  # Layout для анализа расходов
│   ├── Button/          # Кнопка
│   ├── Calendar/        # Компонент календаря
│   ├── ExpensesLayout/  # Layout для расходов
│   ├── Header/          # Шапка приложения
│   ├── Hero/            # Основной контейнер
│   ├── Histogram/       # Компонент гистограммы
│   ├── login/           # Компонент аутентификации
│   ├── NewExpense/      # Форма добавления расхода
│   └── ExpenseTable/    # Таблица расходов
├── context/             # React Context
│   └── AuthContext.jsx  # Контекст аутентификации
├── data.js              # Примерные данные расходов
├── hooks/               # Пользовательские хуки
│   └── useAuth.js       # Хук для аутентификации
├── pages/               # Страницы приложения
│   ├── CostAnalysisPage.jsx
│   ├── MainPage.jsx
│   ├── MyExpensesPage.jsx
│   ├── PrivatePage.jsx
│   ├── SignInPage.jsx
│   └── SignUpPage.jsx
├── App.jsx              # Главный компонент
├── AppRoutes.jsx        # Настройка маршрутов
├── main.jsx             # Точка входа
└── *.styled.js          # Стилизованные компоненты
```

Подключение API:
-- получение списка транзакций,
-- редактирование транзакции по ID,
-- удаление транзакции по ID,
-- фильтрация транзакций по выбранным категориям,
-- фильтрация транзакций по дате или сумме

Фильтры транзакций:
-- реализована логика фильтра по выбранным категориям:
--- реализовано открытие/закрытие фильтра
--- реализован выбор категории (активное/неактивное состояние при клике на категорию)
--- реализовано отображение списка транзакций, согласно выбранным категориям, через API
--- стиль оформления реализован согласно макету

-- реализована логика фильтра по дате и сумме транзакций:
--- реализован выбор категории (активное/неактивное состояние при клике на категорию)
--- реализовано отображение списка транзакций, согласно выбранным датам или сумме, через API
--- стиль оформления реализован согласно макету

Транзакции:
-- реализована логика добавления новой транзакции:
--- настроены инпуты с форматом данных как в макете, и переводом данных в формат для передачи на сервер

-- реализована логика редактирования транзакции:
--- реализована функция редактирования транзакции по id, все данные транзакции передаются в инпут для дальнейшего редактирования

-- реализована логика удаления транзакции по iD

Валидация формы:
-- реализовано отображение ошибок получаемых с сервера
-- реализована валидация полей формы, согласно макету и описанию API при отправке данных на сервер
-- реализована защита от вставки кода в поля ввода формы
-- реализовано цветовое отображение согласно макету, красная подсветка форма при неверном значении меняется на зеленую при правильном вводе данных

Внешний вид таблицы расходов:
-- реализовано правильное отображение суммы согласно макету, в таблице транзакций и в форме добавления новой транзакции
